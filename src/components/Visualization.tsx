import React from "react";
import styled from "styled-components";

interface VisualizationProps {}

class Visualization extends React.Component<VisualizationProps, object> {
  render() {
    return <div />;
  }
}

export default Visualization;
